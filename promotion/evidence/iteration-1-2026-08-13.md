# Iteration 1 — terminal captures, 2026-08-13

Fresh `git clone --depth 20` of `HomenShum/NodeProof` at `9a837ee`, Windows 11,
Node v22.22.2, npm 10.9.7, Playwright 1.62.1. Every command below is a committed
npm target or a committed script. The browser half lives in
[browser-proof/](browser-proof/).

## The defect, before

The product's own readiness command reported that it could not prove a browser.

```
$ npm run demo            # = npx proofloop doctor --json
  "playwright": {
    "declared": false,
    "configExists": false
  },
  "ready": false,
  "missing": [
    ".proofloop/manifest.json",
    "agent instructions (AGENTS.md, CLAUDE.md, Cursor, or Windsurf)",
    "Playwright/browser proof dependency or config"
  ],
  "fixes": [
    "npx proofloop init --agent auto --live",
    "npx proofloop init --agent all --live",
    "npm i -D @playwright/test && npx playwright install"
  ]
```

Meanwhile two committed scripts already imported the package the manifest did not
declare — `scripts/hosted-worker.mjs:37` and `scripts/record-gate-demo.mjs:33`
both `await import("playwright")` and print "install it yourself first" when it
is absent, and `.github/workflows/hosted-proofloop.yml:42` installs it with
`npm install --no-save playwright@1.49.1`. The dependency was real and pinned in
a workflow file rather than in `package.json`.

## The defect, after

```
$ npm run demo
  "playwright": {
    "declared": true,
    "configExists": false
  },
  "ready": false,
  "missing": [
    ".proofloop/manifest.json",
    "agent instructions (AGENTS.md, CLAUDE.md, Cursor, or Windsurf)"
  ],
  "fixes": [
    "npx proofloop init --agent auto --live",
    "npx proofloop init --agent all --live"
  ]
```

`ready` is still `false` — the two remaining gaps are real and are not this
iteration's subject. The Playwright/browser line is gone, which is the product
reporting, about itself, that the defect is closed.

## The probe

```
$ node scripts/browser-proof.mjs               # default port
port 4310 is already in use — another server would be measured instead of this
one. Re-run with --port <free port>.
EXIT=2

$ node scripts/browser-proof.mjs --port 4311
PASS browser proof -> promotion/evidence/browser-proof/receipt.json
  ! open defect (recorded, not fixed): D1 public/app.js:127 renders the machine enum "blocked" as the user-facing headline
  ! open defect (recorded, not fixed): D2 [data-intake-status] has no role="status"/aria-live, so the only dynamic output is announced to nobody
PROBE_EXIT=0
```

The first run is kept on purpose: it is the guard working. Wave 1 lost time to
two servers sharing a port across address families without `--strictPort`
noticing, so this probe binds `127.0.0.1` explicitly and exits 2 rather than
measuring a stranger's page.

## Condition 11 — build, tests, gate

```
$ npm run build
> tsc -p tsconfig.json && node scripts/build-site.mjs
built public/webcontainer-demo.bundle.js + public/xterm.css
BUILD_EXIT=0

$ npm test                       # pretest runs build again
 Test Files  27 passed (27)
      Tests  149 passed (149)
   Duration  26.17s
TEST_EXIT=0

$ node dist/cli.js gate
proofloop gate: PASSED
  source: config-checks
  checks:
    - [pass] build (5741ms)
    - [pass] tests (24151ms)
GATE_EXIT=0
```

Baseline was 26 files / 145 tests; this iteration adds `tests/browserProof.test.ts`
(4 tests). **All four were confirmed failing on the pre-fix tree** by stashing
`package.json`, `package-lock.json`, `scripts/browser-proof.mjs` and
`promotion/evidence/browser-proof/`, then running
`npx vitest run tests/browserProof.test.ts`:

```
 Test Files  1 failed (1)
      Tests  4 failed (4)
   expected false to be true          # scripts/browser-proof.mjs missing
   ENOENT ... browser-proof/receipt.json
```

## `npm run proof` — maturity, unchanged by this iteration

```
$ npm run proof                  # = npx proofloop maturity --dense
level=5 status=partial title=Agent OS / benchmark-ready
capability=live_browser_verification level=3 status=met
missing=Official benchmark adapters: ...
missing=Model sweep and cost leaderboard: ...
missing=Memory and session mining: ...
```

`live_browser_verification` already read `met` before this change — it matches on
`scripts/hosted-worker.mjs` existing, not on anything being runnable. That is a
separate weakness in `src/maturity.ts:270` and is **not** claimed as fixed here.

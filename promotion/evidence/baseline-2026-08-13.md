# Baseline captures — 2026-08-13

Raw output from the Wave 1 baseline run. Every number quoted in
[../PRODUCT_GOAL.md](../PRODUCT_GOAL.md) and [../PROMOTION_LOG.md](../PROMOTION_LOG.md)
comes from this file. Nothing here was reconstructed after the fact.

> **Corrected 2026-08-13 — read this first.** The lines below are left exactly as
> recorded, but two things about them are now known. (1) The browser sections
> (§J4, §J5, conditions 3/4/6/9/10) cite screenshot handles like `ss_5046tck1a`
> that exist only inside the session that took them, and no probe that produced
> the measurements was committed — so the rows they backed are UNVERIFIED, not
> PASS. (2) The §J5 / condition-9 line `POST /api/hosted/submit  200` is wrong:
> `api/hosted/submit.js` has no 200 path (`:33` → 400 blocked, `:47` → 503,
> `:58` → 202 queued). Retained, re-runnable output lives in
> [rerun-2026-08-13.md](rerun-2026-08-13.md); the full accounting is in
> [../PROMOTION_LOG.md](../PROMOTION_LOG.md) § Correction — 2026-08-13.

- Repo: `HomenShum/NodeProof` @ `ea2eb77`, fresh `git clone --depth 50`
- Host: Windows 11, Node v22.22.2, npm 10.9.7
- Web surface served at `http://127.0.0.1:4231` by a local harness that mounts
  `public/` plus the repo's own `api/**/*.js` handlers (Vercel functions run
  unmodified). `vercel dev` not used — it needs an account login.

## §J1 — clone, install, build, test

```
$ npm install --no-audit --no-fund
added 52 packages in 11s
EXIT=0

$ npm run build
> tsc -p tsconfig.json && npm run build:site
> node scripts/build-site.mjs
built public/webcontainer-demo.bundle.js + public/xterm.css
BUILD_EXIT=0

$ npm test          # pretest runs build again
 RUN  v4.1.9
 Test Files  26 passed (26)
      Tests  145 passed (145)
   Duration  57.94s
TEST_EXIT=0
```

## §J2 — the gate refuses, then passes

Throwaway project: `add.js` returning `a - b`, `test.js` asserting `add(2,2) === 4`.

```
$ node <repo>/dist/cli.js init
proofloop init: detected generic web app (no framework markers detected).
proofloop init: wrote .../throwaway/proofloop.config.json
proofloop init: wrote .../throwaway/.proofloop/manifest.json
INIT_EXIT=0

$ node <repo>/dist/cli.js gate            # add() is still wrong
proofloop gate: no checks configured; falling back to `npm test` (package.json has a test script).
FAIL: add(2,2) = 0
proofloop gate: check "npm test" FAILED (1831ms, exit 1)
proofloop gate: FAILED
  state: .../throwaway/.proofloop/gate-state.json
  source: npm-test-fallback
GATE1_EXIT=1

# one-line fix: return a + b

$ node <repo>/dist/cli.js gate
ok
proofloop gate: check "npm test" PASSED (1731ms, exit 0)
proofloop gate: PASSED
GATE2_EXIT=0

$ node <repo>/dist/cli.js resume --dense
status=passed
gate=.proofloop/gate-state.json
checks=1
failed=none
next=ship or install CI with `npx proofloop ci install github` if not already present
RESUME_EXIT=0
```

## §J3 — zero-key doctor and maturity

```
$ npm run demo            # = npx proofloop doctor --json
  "playwright": { "declared": false, "configExists": false },
  "browserReady": true,
  "githubWorkflowExists": true,
  "uiContractsFound": 4,
  "ready": false,
  "missing": [
    ".proofloop/manifest.json",
    "agent instructions (AGENTS.md, CLAUDE.md, Cursor, or Windsurf)",
    "Playwright/browser proof dependency or config"
  ],
  "fixes": [ "npx proofloop init --agent auto --live", ... ]
DEMO_EXIT=0

$ npm run proof           # = npx proofloop maturity --dense
repo=proofloop  currentLevel=4  currentStage=Long-running proof loop  score=93
level=5 status=partial title=Agent OS / benchmark-ready
capability=official_benchmark_adapters level=5 status=partial
capability=model_sweep_costing level=5 status=partial
missing=Official benchmark adapters: ...
missing=Model sweep and cost leaderboard: ...
PROOF_EXIT=0
```

## §J4 — GitHub repo intake, driven in Chrome

Input `https://github.com/HomenShum/NodeProof` → click `[data-testid="target-submit"]`.
Accessibility tree after the click:

```
main
 generic "ProofLoop Live"
 heading "ProofLoop your app or repo"
 search
  textbox "URL or GitHub repository"
  button "ProofLoop"
 link "Continue with GitHub" href="/api/auth/github/start"
 generic "GitHub repo target ready."
 generic "git clone https://github.com/HomenShum/NodeProof && cd NodeProof && npx proofloop init --agent auto --live && npx proofloop maturity --target-level 5 --write && npx proofloop gate"
```

Screenshot: `ss_5046tck1a` (1512x812).

## §J5 — live-URL intake refuses an unverified host

Input `https://example.com` → click submit → POST `/api/hosted/submit` → 200 with
a refusal packet. Rendered:

```
status text: "blocked"          <- D1: bare machine token as the whole message
detail:
{
  "status": "pending",
  "host": "example.com",
  "method": "well-known-token",
  "token": "proofloop-domain-example-com-verify",
  "evidence": [
    "Serve https://example.com/.well-known/proofloop-domain-verification.txt containing proofloop-domain-example-com-verify.",
    "Or publish DNS TXT _proofloop.example.com=proofloop-domain-example-com-verify."
  ],
  "blockers": [ ... ]
}
```

Screenshot: `ss_1809mprwv` (desktop), `ss_6450uratp` (390 px frame beside desktop).

## Conditions 3 and 4 — measured layout, not read CSS

`document.documentElement` measured in a real viewport at each width. The page
was re-rendered at each width; the 386 px row was additionally measured with the
blocked-state JSON detail visible, the widest content the page ever shows.

| viewport | scrollWidth | clientWidth | horizontal overflow |
|---|---|---|---|
| 316 | 316 | 316 | none |
| 386 | 386 | 386 | none |
| 386 (blocked state rendered) | 386 | 386 | none |
| 764 | 764 | 764 | none |
| 2560 | 2560 | 2560 | none |

Breakpoint doing real work (`@media (max-width: 620px)`), at 386 px:

```
input/button stacked: true      (button.top > input.top, i.e. two rows)
button:  344 x 48   (full width)
input:   344 wide
github sso: 362 x 38 (full width)
h1 font-size: 32px  (clamp floor)
```

At 2560 px the same elements sit on one row: `input 613 wide`, `button 119 x 48`,
both at `top 771`.

## Condition 6 — keyboard and the missing live region

Tab order observed with real Tab keypresses on a foregrounded tab:

```
Tab 1 -> BUTTON  data-testid="target-submit"   outline: 2px solid rgb(229,149,121)
Tab 2 -> A       data-testid="github-sso"      "Continue with GitHub"
```

(The input holds focus first via `autofocus`, so the first Tab moves off it.)

The status region, read from the live DOM:

```
document.querySelector('[data-intake-status]').outerHTML
=> '<p class="status" data-intake-status="" hidden=""></p>'
```

No `role`, no `aria-live` — D2.

## Condition 9 — console and network

Console, whole session (load + both journeys): 6 messages, **all** from
`chrome-extension://nkbihfbeogaeaoehlefnkodbefgpgknn/scripts/contentscript.js`
(a wallet extension) — `MaxListenersExceededWarning` x2, `ObjectMultiplex -
orphaned data for stream ...` x4. Zero messages from the page origin. Zero
errors.

Network, same-origin requests only (extension-origin requests omitted):

```
GET http://127.0.0.1:4231/                        200
GET http://127.0.0.1:4231/styles.css              200
GET http://127.0.0.1:4231/app.js                  200
GET http://127.0.0.1:4231/api/auth/github/status  200
POST http://127.0.0.1:4231/api/hosted/submit      200 (refusal packet, §J5)
```

`/api/auth/github/status` body: `{"ok":true,"provider":"github","authConfigured":false,"authenticated":false,"user":null}`
— fails closed with an explicit unconfigured status rather than pretending
sign-in works, matching the README.

## Condition 10 — timings

```
domContentLoadedEventEnd: 317 ms
loadEventEnd:            1224 ms
document transfer:      1867 B
/styles.css:            3931 B  (49 ms)
/app.js:                5534 B  (52 ms)
total same-origin:     ~11.3 KB
```

No paint entries were recorded because the automation tab was backgrounded at
capture time; the timings above and the observed immediacy of every click's state
change are what condition 10 rests on. Core Web Vitals proper belong to condition
8, which was not audited.

## D3 — the orphaned bundle

```
$ ls -la public/
 5234  app.js
 1567  index.html
 3631  styles.css
361920 webcontainer-demo.bundle.js     <- built by scripts/build-site.mjs
  7112 xterm.css                       <- built by scripts/build-site.mjs

$ grep -rn "webcontainer" public/index.html public/app.js tests/ scripts/build-site.mjs
scripts/build-site.mjs:9:  entryPoints: [join(root, "site-src", "webcontainer-demo.js")],
scripts/build-site.mjs:15:  outfile: join(root, "public", "webcontainer-demo.bundle.js"),
scripts/build-site.mjs:24:console.log("built public/webcontainer-demo.bundle.js + public/xterm.css");
```

The only references are the build step that creates them. No page loads them, no
test asserts them.
